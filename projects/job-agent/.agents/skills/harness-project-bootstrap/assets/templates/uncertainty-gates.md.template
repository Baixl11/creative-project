# Uncertainty Gates Protocol

本文件定义重要流程中的“无法完全确认就暂停”规则。目标是防止 Codex 在缺少关键事实时靠猜测继续，导致 harness 规则、环境配置、实现方案或验收标准越跑越偏。

## 核心规则

- 初始化项目分析、需求分析、问题拆解、环境配置、验证命令、数据影响、安全边界、依赖策略、视觉验收、git 分支/提交策略等重要流程，如果无法从用户输入、仓库文件、现有配置或已执行验证中完全确认，必须暂停并向用户提问。
- 关键未知项不得只写入 `assumptions` 后继续执行。
- 只有低风险、可逆、不影响行为/数据/安全/环境/验收结论的细节，才允许作为假设继续，并必须记录在 `problem-decomposition.md` 或 `harness-bootstrap-report.md`。

## 触发条件

出现以下情况必须进入不确定性闸门：

- 项目技术栈、启动方式、构建方式、测试方式或包管理器存在多个可能解释。
- dev/build/test/lint/package/release 命令涉及父级或兄弟项目，但相关项目是否参与、是否存在 `.harness`、验证顺序或失败归属无法确认。
- 需求目标、非目标、交互边界、视觉还原标准或验收阈值不可判断。
- 问题拆解中的当前状态、期望状态、约束、方案选择或工作包边界缺少关键证据。
- 环境配置涉及本机路径、系统依赖、端口、证书、账号、密钥、外部服务、模型服务或网络访问。
- 前端入口、Electron 启动方式、测试数据、浏览器/窗口尺寸、MCP Playwright 或项目 Playwright 可用性无法确认。
- 验证命令可能产生副作用、依赖外部服务、需要大量时间、需要凭证，或项目中存在多个候选命令。
- 修改会影响数据库、文件持久化、权限、安全边界、IPC、支付、生产依赖、公共 API 或迁移逻辑。
- git 分支、提交粒度、是否提交、是否覆盖已有文件、是否推送或是否处理未归属改动无法确认。
- 视觉任务的参考尺寸、目标视口、缩放策略、差异严重度或是否可接受无法判断。

## 执行方式

1. 先补充读取直接证据，不能因为没有立刻看到答案就马上提问。
2. 如果问题或方案仍不明确，先按 `research-protocol.md` 做本地调研和必要外部资料检索，并写 `research-notes.md`。
3. 如果调研后仍无法完全确认，停止进入下一阶段。
4. 如果任务目录已经存在，更新 `state.json.status` 为 `needs_clarification` 或 `blocked_on_decision`。
5. 在 `events.ndjson` 追加 `clarification_requested` 事件，并在 `summary.md`、`handoff.md` 写明阻塞点。
6. 向用户提出不超过 3 个问题；每个问题必须说明影响范围和默认推荐选项。
7. 用户确认后，把答案写入 `decision-log.md`、`problem-decomposition.md` 或 `harness-bootstrap-report.md`，再继续执行。

## 提问格式

问题必须短、具体、可回答：

```text
当前存在一个会影响实现方向的不确定点：项目里同时存在 pnpm 和 npm lockfile。请确认后续以哪个包管理器为准？
推荐：pnpm，因为 packageManager 字段声明了 pnpm@x.y.z。
影响：会决定安装、构建、锁文件和 CI 规则。
```

避免问泛泛问题，例如“你希望我怎么做？”。必须把已知证据、风险和可选路径讲清楚。

## 状态约束

- `needs_clarification`：缺少用户输入才能继续分析、规划、编码或验证。
- `blocked_on_decision`：已有多个方案，但无法确定应采用哪一个。
- 存在上述状态时，任务不得进入 `coding`、`needs_verification` 或 `ready_for_human_review`。
- 用户回复后必须先清理阻塞状态，再进入后续阶段。

## 与假设的边界

允许记录为假设并继续：

- 不影响外部行为的文案组织。
- 可逆的小范围文档补充。
- 与项目现有证据一致且不会改变验证结论的命名推断。

必须暂停询问：

- 会改变代码行为、数据结构、接口、视觉验收、依赖、验证命令、运行环境或 git 历史的选择。
- 会改变存在 `.harness` 的独立 related project，或无法确认 `.harness` 状态的父级/兄弟项目代码、配置、依赖或构建顺序的选择。
- 无法通过本地证据确认，但错误选择会导致返工、破坏数据、误判验证或覆盖用户已有工作的事项。

## Bootstrap 阶段规则

bootstrap 尚未创建任务目录时，仍必须执行不确定性闸门：

- 暂停生成或渲染文件。
- 直接向用户提问。
- 用户确认后，把答案写入 `.harness/bootstrap-plan.yaml` 和 `docs/design-docs/harness-bootstrap-report.md`。
- 如果用户暂不确认，报告中必须把对应文件、命令或规则标记为 skipped 或 deferred，不得伪装成已确定。
