# Feedback Flywheel

这里记录值得反馈回共享基础设施的经验。

每次任务完成后，`doc_gardener` 应优先沉淀：

- 反复出现的上下文缺口。
- 反复出现的问题拆解缺口，例如目标不清、非目标缺失、方案取舍缺失或验收标准不可判断。
- 反复出现的调研缺口，例如没有核对官方文档、没有结合当前项目证据或直接套用外部方案。
- 反复出现的关键未知项，例如环境、验证命令、依赖策略、数据影响或视觉验收标准被猜测执行。
- 反复出现的运行时验证缺口，例如只跑 build、没有打开真实应用、没有验证关键交互、没有确认 MCP Playwright/Playwright 是否可用。
- 反复出现的失败模式。
- 需要进入 `AGENTS.md`、`ARCHITECTURE.md` 或 skill references 的长期约束。
- 需要进入验证流程的新传感器。
- 需要进入 `uncertainty-gates.md` 或 `human-gates.yaml` 的人工确认项。
- 需要删除或降级的过时 harness 规则。

## 最近反馈

暂无。
