# 任务摘要

状态：coding

目标：把当前 Job Agent 项目同步到 `git@github.com:Baixl11/creative-project.git`，采用 `projects/job-agent/` 子目录结构，方便未来继续放入其他项目。

当前方案：在临时目录克隆远端仓库，保留远端已有内容，将当前项目同步到 `projects/job-agent/`，根目录维护作品集项目索引。

已完成：本地项目公开化清理、公开中文 demo 兜底、作品集 PDF 更新、临时 monorepo 同步和验证。

下一步：提交并 push 到 GitHub。
