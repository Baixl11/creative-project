# 用户需求

用户希望把当前 Job Agent 的 Web 入口升级成“网页即点即用”的形式，而不是要求使用者先打开终端运行 `streamlit run web_app.py`。

同时，修改完成后需要同步到 GitHub 仓库：

- `git@github.com:Baixl11/creative-project.git`

仓库采用多项目结构，因此 Job Agent 仍应位于：

- `projects/job-agent/`
